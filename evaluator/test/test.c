#include "test.h"

int test_eval()
{
	/*
	/// Создание структуры для хранения выражения:
	Expression *expression = create_expression();

	/// Пример выражения: (def x 10):
	Symbol *def_symbol = create_symbol("def");

	ExpressionElement *def_element = create_expression_element(ELEMENT_SYMBOL, def_symbol);

	append_to_expression(expression, def_element);


	Symbol *x_symbol = create_symbol("x");

	ExpressionElement *x_element = create_expression_element(ELEMENT_SYMBOL, x_symbol);

	append_to_expression(expression, x_element);

	Value *value = create_value(sizeof(long long), malloc(sizeof(long long)));

	*(long long *)(value->value) = 10;

	ExpressionElement *value_element = create_expression_element(ELEMENT_VALUE, value);

	append_to_expression(expression, value_element);

	/// Выполнение выражения и получение результата:
	Value *result = eval(expression);

	if(result != NULL) {
		printf("Result: %lld\n\n", *(long long *)result->value);

		free_value(result);
	} else {
		printf("No Result Or Invalid Expression!!!\n\n");
	}

	/// Освобождение памяти:
	free_expression(expression);
	*/

	return 0;
}
